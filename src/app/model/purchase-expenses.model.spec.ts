import { PurchaseExpenses } from './purchase-expenses.model';

describe('PurchaseExpenses', () => {
  it('should create an instance', () => {
    expect(new PurchaseExpenses()).toBeTruthy();
  });
});
